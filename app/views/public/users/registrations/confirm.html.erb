<div class="contents public-users-registrations-confirm">
  <h2>入力情報の確認</h2>
  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <div class="confirm-form">
      <h3>お名前</h3>
      <p>予約時の情報として使用されます。</p>
      <div class="confirm-form__name flex">
        <div class="form-group">
          <%= f.label :name_family %>：<%= f.text_field :name_family, value: @user.name_family, readonly: true %>
        </div>
        <div class="form-group">
          <%= f.label :name_first %>：<%= f.text_field :name_first, value: @user.name_first, readonly: true %>
        </div>
      </div>
      <div class="confirm-form__name-kana flex">
        <div class="form-group">
          <%= f.label :name_family_kana %>：<%= f.text_field :name_family_kana, value: @user.name_family_kana, readonly: true %>
        </div>
        <div class="form-group">
          <%= f.label :name_first_kana %>：<%= f.text_field :name_first_kana, value: @user.name_first_kana, readonly: true %>
        </div>
      </div>
      <div class="confirm-form__number-email">
        <div class="form-group">
          <h3><%= f.label :phone_number %></h3>
          <%= f.text_field :phone_number, value: @user.phone_number, readonly: true %>
        </div>
        <div class="form-group">
          <h3><%= f.label :email %></h3><p>ログイン時のIDとして使用されます。また、予約の確認や、メールマガジンの配信も行われます。</p>
          <%= f.email_field :email, value: @user.email, readonly: true %>
        </div>
      </div>
      <h3><%= f.label :password %></h3>
      <div ckass="confirm-form__password">
        <div class="formgroup">
          <%= blind(@user.password) %>
          <%= f.hidden_field :password, value: @user.password %>
        </div>
      </div>
      <p>
        よろしければ[仮登録]を押してください。<br>
        ご入力いただいたメールアドレス宛に確認メールを送信いたします。
      </p>
      <div class="confirm-form__submit">
        <div class="form-group">
          <%= f.submit "修正する", class: 'btn btn-outline-dark', name: :back %>
          <%= f.submit "仮登録", class: 'btn btn-success' %>
        </div>
      </div>
    </div>
  <% end %>
  <%= render "public/users/shared/links" %>
</div>
