<div class="contents public-users-registrations-new">
  <h2>新規会員登録</h2>
  <%= render partial: 'shared/messages/flash_messages' %>
  <%= form_with model: @user, url: users_sign_up_confirm_path(@user), local: true do |f| %>
    <div class="registration-form">
      <h3>お名前</h3>
      <p>予約時の情報として使用されます。</p>
      <div class="registration-form__name flex">
        <div class="form-group">
          <%= f.label :name_family %>
          <%= f.text_field :name_family, required: true, id: "new-user-name_family", class: "form-control #{("error-form" if @user.errors.include?(:name_family))}" %>
          <%= render partial: 'shared/messages/each_error_messages', locals: { model: @user, key: :name_family } %>
        </div>
        <div class="form-group">
          <%= f.label :name_first %>
          <%= f.text_field :name_first, required: true, id: "new-user-name_first", class: "form-control #{("error-form" if @user.errors.include?(:name_first))}" %>
          <%= render partial: 'shared/messages/each_error_messages', locals: { model: @user, key: :name_first } %>
        </div>
      </div>
      <div class="registration-form__name-kana flex">
        <div class="form-group">
          <%= f.label :name_family_kana %>
          <%= f.text_field :name_family_kana, required: true, id: "new-user-name_family_kana", class: "form-control #{("error-form" if @user.errors.include?(:name_family_kana))}" %>
          <%= render partial: 'shared/messages/each_error_messages', locals: { model: @user, key: :name_family_kana } %>
        </div>
        <div class="form-group">
          <%= f.label :name_first_kana %>
          <%= f.text_field :name_first_kana, required: true, id: "new-user-name_first_kana", class: "form-control #{("error-form" if @user.errors.include?(:name_first_kana))}" %>
          <%= render partial: 'shared/messages/each_error_messages', locals: { model: @user, key: :name_first_kana } %>
        </div>
      </div>
      <div class="registration-form__number-email">
        <div class="form-group">
          <h3><%= f.label :phone_number %>(ハイフンなし)</h3>
          <p>予約後に店舗よりご連絡する場合がございます。</p>
          <%= f.number_field :phone_number, min: 0, required: true, id: "new-user-phone_number", class: "form-control #{("error-form" if @user.errors.include?(:phone_number))}" %>
          <%= render partial: 'shared/messages/each_error_messages', locals: { model: @user, key: :phone_number } %>
        </div>
        <div class="form-group">
          <h3><%= f.label :email %></h3>
          <p>ログイン時のID、ご予約の確認、メールマガジンの配信に使用します。(後から変更できます。)</p>
          <%= f.email_field :email, autocomplete: "email", required: true, place_holder: 'example@example.com', id: "new-user-email", class: "form-control #{("error-form" if @user.errors.include?(:email))}" %>
          <%= render partial: 'shared/messages/each_error_messages', locals: { model: @user, key: :email } %>
        </div>
      </div>
      <h3>パスワード(半角英数字6文字以上)</h3>
      <div class="registration-form__password">
        <div class="form-group">
          <p><%= f.label :password, 'ログイン時に使用します。'%></span>
          <%= f.password_field :password, autocomplete: "new-password", required: true, id: "new-user-password", class: "form-control #{("error-form" if @user.errors.include?(:password))}" %>
          <%= render partial: 'shared/messages/each_error_messages', locals: { model: @user, key: :password } %>
        </div>
        <div class="form-group">
          <%= f.label :password_confirmation, '確認のため、再入力してください。' %>
          <%= f.password_field :password_confirmation, autocomplete: "new-password", required: true, id: "new-user-password_confirmation", class: "form-control #{("error-form" if @user.errors.include?(:password_confirmation))}" %>
          <%= render partial: 'shared/messages/each_error_messages', locals: { model: @user, key: :password_confirmation } %>
        </div>
      </div>
      <div class="registration-form__submit">
        <div class="form-group">
          <%= f.submit "確認する", class: 'btn btn-success' %>
        </div>
      </div>
    <div>
  <% end %>
  <%= render "public/users/shared/links" %>
</div>
